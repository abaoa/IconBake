// Type stub for clipper-lib (ships no type definitions).
// We only touch ClipperOffset / Clipper.SimplifyPolygons, so `any` is enough.
declare module 'clipper-lib' {
  const ClipperLib: any
  export default ClipperLib
}
